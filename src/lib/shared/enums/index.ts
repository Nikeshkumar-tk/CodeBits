export enum IdentityProviders {
    CODEBITS = "CODEBITS",
    GOOGLE = "GOOGLE",
  }
  
  export enum MONGO_COLLECTIONS {
    SNIPPETS = "Snippet",
    SNIPPET_LANGUAGES_CONFIG = "SnippetLanguages"
  }
  
  export enum USER_ROLES {
    USER = "user",
    ADMIN = "admin",
  }
  
  export enum HTTP_METHODS {
    POST = "POST",
    GET = "GET",
    PATCH = "PATCH",
    PUT = "PUT",
  }
  